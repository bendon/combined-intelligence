# Copy to /etc/combined-intelligence/flower.env (chmod 600) and reference from
# ci-flower.service. Keep separate from backend/.env so systemd parses cleanly.
FLOWER_USER=admin
FLOWER_PASSWORD=change-me
